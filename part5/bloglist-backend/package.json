{
  "name": "blog",
  "version": "1.0.0",
  "description": "blog exercise for fullstack",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": " NODE_ENV=development node --watch index.js",
    "dev-local": "NODE_ENV=dev-local node --watch index.js",
    "test": "NODE_ENV=test node --test --test-concurrency=1",
    "test-local": "NODE_ENV=test-local node --test --test-concurrency=1",
    "lint": "eslint .",
    "lint-fix": "eslint . --fix",
    "start:test": "cross-env NODE_ENV=test node --watch index.js",
    "start:test-local": "cross-env NODE_ENV=test-local node --watch index.js"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cross-env": "^10.1.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "lodash": "^4.18.1",
    "mongoose": "^9.7.4"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.10.0",
    "eslint": "^10.7.0",
    "globals": "^17.7.0",
    "supertest": "^7.2.2"
  }
}
